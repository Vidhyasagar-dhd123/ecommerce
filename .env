# ============================================================
#  .env — Local Development Environment
#  DO NOT commit secrets from production here.
#  Copy this file and fill in real values for staging/prod.
# ============================================================

# Django core
DJANGO_SECRET_KEY=django-insecure-vvm2um_%qk@u!y(@+@zq0(&*ip(h_lthm*vbi7mh(sk9e%1(=r
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173

# PostgreSQL
DB_NAME=ecommerce_db
DB_USER=ecommerce_user
DB_PASSWORD=ecommerce
DB_HOST=localhost
DB_PORT=5432

# Throttle rates (override to tighten in staging/prod)
DJANGO_THROTTLE_ANON=60/minute
DJANGO_THROTTLE_USER=300/minute
DJANGO_THROTTLE_AUTH=10/minute
